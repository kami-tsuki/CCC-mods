name: KamiGeology

on:
  push:
    branches: [main, master, develop, 'release**']
    paths:
      - 'KamiGeology/**'
      - 'KamiLibs/**'
      - 'build.gradle.kts'
      - 'settings.gradle.kts'
      - 'gradle.properties'
      - 'gradle/**'
      - '.github/workflows/_mod-ci.yml'
      - '.github/workflows/KamiGeology.yml'

jobs:
  ci:
    uses: ./.github/workflows/_mod-ci.yml
    with:
      mod: KamiGeology
